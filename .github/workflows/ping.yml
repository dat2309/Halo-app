name: Keep Render Awake

on:
  workflow_dispatch:

  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render API
        run: curl --silent --fail https://halo-app-twrt.onrender.com/api/health
